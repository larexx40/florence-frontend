# API Configuration
# Use /api for dev (proxied via Vite to bypass SSL issues)
# Update to full URL for production: https://api.everythingflorences.com/api
VITE_APP_API_BASE_URL=/api

# API Key (will be implemented when backend requires it)
# VITE_APP_API_KEY=your-api-key-here
